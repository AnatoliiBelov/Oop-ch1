package transport;

public class Bus extends Transport {
    public Bus(String brand, String model, int year, String contry, String color, int maxSpeed) {
        super(brand, model, year, contry, color, maxSpeed);
    }

}
